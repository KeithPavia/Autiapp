
  # Daily Routine Home Screen

  This is a code bundle for Daily Routine Home Screen. The original project is available at https://www.figma.com/design/9wnkmhS6dwo0fQPO69KKWe/Daily-Routine-Home-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  

## Custom icon replacement
This version uses files in `public/custom-images/icons/` for the app's replaceable icons. Keep the same filenames when swapping your own icon images.
